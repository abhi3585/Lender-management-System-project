import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BorrowerLoanDetailsComponent } from './borrower-loan-details.component';

describe('BorrowerLoanDetailsComponent', () => {
  let component: BorrowerLoanDetailsComponent;
  let fixture: ComponentFixture<BorrowerLoanDetailsComponent>;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [BorrowerLoanDetailsComponent]
  //   });
  //   fixture = TestBed.createComponent(BorrowerLoanDetailsComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
