import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BorrowerApplicationFormComponent } from './borrower-application-form.component';

describe('BorrowerApplicationFormComponent', () => {
  let component: BorrowerApplicationFormComponent;
  let fixture: ComponentFixture<BorrowerApplicationFormComponent>;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [BorrowerApplicationFormComponent]
  //   });
  //   fixture = TestBed.createComponent(BorrowerApplicationFormComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
